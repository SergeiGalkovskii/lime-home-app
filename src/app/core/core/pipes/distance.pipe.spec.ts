import { DistancePipe } from './distance.pipe';

describe('DistanceNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DistancePipe();
    expect(pipe).toBeTruthy();
  });
});
